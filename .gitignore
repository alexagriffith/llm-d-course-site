# Never commit video bytes — they live in Google Drive / a CDN, not in git.
*.mp4
*.mov
*.webm
*.gif
*.MP4
*.MOV

# OS cruft
.DS_Store

# local preview only
media
chapters.local.json

# design-system source kit stays local; only ds-bundle.js ships
_design-system/
